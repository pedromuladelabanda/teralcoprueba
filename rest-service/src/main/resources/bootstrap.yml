spring:
  cloud:
    consul:
      discovery:
        preferIpAddress: true
        instanceId: ${spring.application.name}:${random.value}
      config:
        name: restService
      host: localhost
      port: 32598